get(/API/CATEGORIES)

-> [{_id: ..., name: ..., parentCategory:...}, {...}, {...}]

arr.map((item)=> link:item.name )
/categories/<categoryName>

products.filter (product.category.name !== params.categoryName)

<Layout -> categories> <-> api:products -> props
 <route  /electronics>
 <route /apparal?>
 ...
</Layout>

1->continue the showing by clicking links
{show the category name and description}

2->filter by price
3->sorting (high and low)